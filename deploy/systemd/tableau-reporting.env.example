# Install as /etc/cms-data/tableau-reporting.env with root ownership and mode 0600.
# The publisher follows only the verified immutable production release-current selection.
CMS_REPORTING_DSN=postgresql://cms_reporting_loader:REPLACE_WITH_GENERATED_SECRET@127.0.0.1:5434/cms_tableau
